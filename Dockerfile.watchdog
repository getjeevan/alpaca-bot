FROM python:3.11-slim

WORKDIR /app

# Install Docker CLI
RUN apt-get update && apt-get install -y --no-install-recommends \
    docker.io \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Copy watchdog script
COPY watchdog-docker.py ./watchdog.py

# Run watchdog
CMD ["python3", "watchdog.py"]
